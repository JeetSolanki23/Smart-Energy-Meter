# Frontend API base used in src/lib/api.ts
# Keep as /api/v1 when using Vite proxy in local development.
VITE_API_BASE=/api/v1

# Dev server settings for Vite
VITE_DEV_HOST=::
VITE_DEV_PORT=8080

# Where Vite should proxy /api requests in development
VITE_API_PROXY_TARGET=http://localhost:8000
